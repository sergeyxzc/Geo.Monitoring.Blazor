@page
@using Geo.Monitoring.Blazor.Services
@attribute [IgnoreAntiforgeryToken]
@inject ILoginUserService LoginUserService
@functions {
    public async Task<IActionResult> OnPost()
    {
        await LoginUserService.SignOutAsync();
        return Redirect("~/");
    }
}
